/*
** EPITECH PROJECT, 2021
** my_defender
** File description:
** my_defender.c
*/

#include "my.h"

/*

sfMusic *opt_music_create(void)
{
    sfMusic *loop_sound;

    loop_sound = sfMusic_createFromFile("assets/music/sec_mus.ogg");
    sfMusic_setLoop(loop_sound, sfTrue);
    return (loop_sound);
}

sfMusic *main_music_create(void)
{
    sfMusic *loop_sound;

    loop_sound = sfMusic_createFromFile("assets/music/main_mus.ogg");
    sfMusic_setLoop(loop_sound, sfTrue);
    return (loop_sound);
}

sfMusic *game_over_music_create(void)
{
    sfMusic *loop_sound;

    loop_sound = sfMusic_createFromFile("assets/music/gameover.ogg");
    sfMusic_setLoop(loop_sound, sfTrue);
    return (loop_sound);
}

void init_sound(defend_t *def, s_mus_t *s_music)
{
    s_music->start_sound = main_music_create();
    def->music.start_sound = s_music->start_sound;
    s_music->second_sound = opt_music_create();
    def->music.second_sound = s_music->second_sound;
    s_music->game_over_sound = game_over_music_create();
    def->music.game_over_sound = s_music->game_over_sound;
}*/
